repos:
- repo: https://github.com/astral-sh/ruff-pre-commit
  # Ruff version.
  rev: v0.15.12
  hooks:
    # Run the linter.
    - id: ruff-check
      args: [ --fix ]
      types_or: [ python, pyi, pyproject ]
    # Run the formatter.
    - id: ruff-format
